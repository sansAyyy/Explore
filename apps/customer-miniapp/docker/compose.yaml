services:
  customer-miniapp:
    image: customer-miniapp:local
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        VITE_GATEWAY_BASE_URL: ${VITE_GATEWAY_BASE_URL:-http://localhost:5203}
    ports:
      - "${CUSTOMER_MINIAPP_PORT:-8081}:80"
